services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES__USER: ${DB_USERNAME}
      POSTGRES__PASSWORD: ${DB_PASSWORD}
      POSTGRES__DB: ${DB_DATABASE}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  
volumes:
  pgdata:

